function fun() {}

let arrNumber = [1, 2, 3, 4, 5, 6, 77, 8];
// Sort the Array
// Sort an array in ascending or descending order (without using built-in sort functions if possible).

// Merge Two Arrays
// Merge two arrays into one new array.

// Find Second Largest Element
// Find the second largest element in a given array.
